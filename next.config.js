/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "ik.imagekit.io",
      "og-image.vercel.app",
      "media3.giphy.com",
      "media0.giphy.com",
      "github.com",
    ],
  },
};

module.exports = nextConfig;
